import { PROVINCES_DISTRICTS } from '@pola/shared';
import { logger } from '../utils/logger.util.js';

export const seedProvincesAndDistricts = async () => {
  logger.info(`🗺️ Sri Lanka reference geography loaded: 9 Provinces, 25 Districts verified.`);
};
